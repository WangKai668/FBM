#!/bin/bash

# 使用find命令查找当前目录（. 表示当前目录）及其子目录下，修改时间早于2024年12月12日的文件
# -mtime +0 表示修改时间大于1天前（也就是早于昨天），这里假设昨天是2024年12月12日
# -type f 表示只查找文件类型，排除目录等其他类型的文件
# -exec 后面跟着的是对找到的每个文件要执行的操作，这里是删除（rm -f）
find . -mtime +0 -type f -exec rm -f {} \;

echo "已删除满足条件的文件。"